#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include <algorithm>
#include <string>
#include <vector>
#include <stack>

using namespace std;
char buffer[20][20];

int main(){
	int w, h;
	while(scanf("%d %d", &w, &h) == 2 && (w || h)){
		for(int i=0; i<n; ++i){
			scanf("%s", buffer[i]);
		}
	}
	return 0;
}
